diff --git a/Xext/meson.build b/Xext/meson.build
index bb90e195d..311485b2c 100644
--- a/Xext/meson.build
+++ b/Xext/meson.build
@@ -62,6 +62,11 @@ if build_xv
     hdrs_xext += ['xvdix.h', 'xvmcext.h']
 endif
 
+if build_tawc
+    srcs_xext += 'tawc-dri.c'
+    hdrs_xext += 'tawcdriproto.h'
+endif
+
 libxserver_xext = static_library('libxserver_xext',
     srcs_xext,
     include_directories: inc,
diff --git a/Xext/tawc-dri.c b/Xext/tawc-dri.c
new file mode 100644
index 000000000..4eb624925
--- /dev/null
+++ b/Xext/tawc-dri.c
@@ -0,0 +1,581 @@
+/*
+ * SPDX-License-Identifier: MIT
+ *
+ * TAWC-DRI X11 extension: Phase 2 step 3.
+ *
+ * Server side of the AHB-shipping protocol. PresentBuffer carries an
+ * Android native_handle_t — numInts ints inline in the request body,
+ * numFds fds out-of-band via X11 FD passing (ReadFdFromClient, the
+ * same machinery DRI3's PixmapFromBuffer uses). The dispatch hands the
+ * pieces to xwl_tawc_present_native_handle, which rebuilds an
+ * AHardwareBuffer via AHardwareBuffer_createFromHandle, ships it
+ * through android_wlegl, and attaches the resulting wl_buffer to the
+ * target X11 window's wl_surface.
+ *
+ * v0.3 adds a server→client event channel (SelectInput + XGE generic
+ * events routed to libxcb special event queues, modelled on the
+ * Present extension): ConfigureNotify so the libhybris X11 platform's
+ * buffer pool follows compositor-driven window resizes, and
+ * BufferRelease so clients get real buffer-lifecycle feedback instead
+ * of a freshness heuristic.
+ *
+ * See notes/xwayland.md.
+ */
+
+#ifdef HAVE_DIX_CONFIG_H
+#include <dix-config.h>
+#endif
+
+#include <stdlib.h>
+#include <stdint.h>
+#include <unistd.h>
+
+#include <X11/X.h>
+#include <X11/Xproto.h>
+
+#include "misc.h"
+#include "os.h"
+#include "dixstruct.h"
+#include "resource.h"
+#include "extnsionst.h"
+#include "extinit.h"
+#include "windowstr.h"
+#include "scrnintstr.h"
+#include "Xext/geext.h"
+
+#include "Xext/tawcdriproto.h"
+
+/* Forward decl — pulling in the Xwayland-private xwayland-tawc.h here
+ * would force an include path the rest of Xext/ doesn't have. The
+ * function is implemented in hw/xwayland/xwayland-tawc.c and resolved
+ * at link time via Xwayland's executable target. */
+extern int xwl_tawc_present_native_handle(WindowPtr window,
+                                          int *fds, int num_fds,
+                                          const int32_t *ints, int num_ints,
+                                          int width, int height, int stride,
+                                          int format, uint64_t usage,
+                                          uint32_t client_mask,
+                                          uint32_t serial);
+
+/* ── Event selections ──
+ *
+ * One record per SelectInput, on a single global list (the population
+ * is a handful of GL clients, not worth per-window privates). Cleanup
+ * is resource-driven, like Present's event contexts:
+ *   - the eid is AddResource'd under the requesting client, so client
+ *     disconnect frees the record;
+ *   - a marker resource under the window's XID frees every record for
+ *     that window when the window is destroyed.
+ */
+typedef struct tawc_dri_event {
+    struct tawc_dri_event *next;
+    ClientPtr client;
+    WindowPtr window;
+    XID eid;
+    CARD32 mask;
+} tawc_dri_event_rec;
+
+static tawc_dri_event_rec *tawc_dri_event_list;
+static RESTYPE tawc_dri_event_type;  /* eid resources */
+static RESTYPE tawc_dri_window_type; /* per-window cleanup marker */
+static int tawc_dri_request;         /* major opcode, for XGE events */
+
+static int
+tawc_dri_free_event(void *data, XID id)
+{
+    tawc_dri_event_rec *event = data;
+    tawc_dri_event_rec **prev, *cur;
+
+    for (prev = &tawc_dri_event_list; (cur = *prev); prev = &cur->next) {
+        if (cur == event) {
+            *prev = event->next;
+            break;
+        }
+    }
+    free(event);
+    return Success;
+}
+
+static int
+tawc_dri_free_window(void *data, XID id)
+{
+    WindowPtr window = data;
+    tawc_dri_event_rec *event, *next;
+
+    for (event = tawc_dri_event_list; event; event = next) {
+        next = event->next;
+        if (event->window == window)
+            FreeResource(event->eid, RT_NONE);
+    }
+    return Success;
+}
+
+static void
+tawc_dri_send_configure_notify_to(tawc_dri_event_rec *event,
+                                  int width, int height)
+{
+    xTAWCDRIConfigureNotify cn = {
+        .type = GenericEvent,
+        .extension = tawc_dri_request,
+        .length = 0,
+        .evtype = TAWCDRIEventConfigureNotify,
+        .eid = event->eid,
+        .window = event->window->drawable.id,
+        .width = width,
+        .height = height,
+    };
+    WriteEventsToClient(event->client, 1, (xEvent *) &cn);
+}
+
+/* ConfigNotify screen hook: dix calls this from ConfigureWindow before
+ * the new geometry is applied, so drawable.width/height still hold the
+ * old size — comparing tells us whether the size actually changes
+ * (position-only moves shouldn't wake GL clients). A size check at
+ * PresentBuffer time would not be enough on its own: a low-fps client
+ * would render a full wrong-sized frame before hearing about it. */
+static ConfigNotifyProcPtr tawc_dri_wrapped_config_notify[MAXSCREENS];
+
+static int
+tawc_dri_config_notify(WindowPtr window,
+                       int x, int y, int w, int h, int bw,
+                       WindowPtr sibling)
+{
+    ScreenPtr screen = window->drawable.pScreen;
+    int ret = Success;
+
+    /* unwrap → call → rewrap, like Present's hooks. Xwayland's own
+     * xwl_config_notify reinstalls itself outermost on every call, so
+     * a wrapper that doesn't rewrap gets silently dropped from the
+     * chain after the first ConfigureWindow. */
+    screen->ConfigNotify = tawc_dri_wrapped_config_notify[screen->myNum];
+    if (screen->ConfigNotify)
+        ret = screen->ConfigNotify(window, x, y, w, h, bw, sibling);
+    tawc_dri_wrapped_config_notify[screen->myNum] = screen->ConfigNotify;
+    screen->ConfigNotify = tawc_dri_config_notify;
+
+    /* Emit only after the wrapped chain accepted the configure — a
+     * failure there aborts the resize, and a client that already
+     * reallocated to the phantom size would render wrong-sized buffers
+     * (the exact black-window class this event exists to fix). The
+     * drawable still holds the old size here (dix applies the new
+     * geometry after ConfigNotify returns), so the change check works. */
+    if (ret == Success &&
+        (w != window->drawable.width || h != window->drawable.height)) {
+        tawc_dri_event_rec *event;
+
+        for (event = tawc_dri_event_list; event; event = event->next) {
+            if (event->window == window &&
+                (event->mask & TAWCDRIConfigureNotifyMask))
+                tawc_dri_send_configure_notify_to(event, w, h);
+        }
+    }
+    return ret;
+}
+
+/* Compositor released a wl_buffer created by a v0.3 PresentBuffer.
+ * Called from hw/xwayland/xwayland-tawc.c's wl_buffer.release
+ * listener. Serials are per-presenting-client, so the release goes
+ * only to that client's selection — matched by clientAsMask + window
+ * XID rather than pointers: if the client or window is gone, its
+ * selections were freed with it and nothing is sent; recycled
+ * identities at worst deliver a stale serial the client ignores. */
+void
+tawc_dri_send_buffer_release(uint32_t window_id, uint32_t client_mask,
+                             uint32_t serial)
+{
+    tawc_dri_event_rec *event;
+
+    for (event = tawc_dri_event_list; event; event = event->next) {
+        if (event->window->drawable.id == window_id &&
+            (uint32_t)event->client->clientAsMask == client_mask &&
+            (event->mask & TAWCDRIBufferReleaseMask)) {
+            xTAWCDRIBufferRelease br = {
+                .type = GenericEvent,
+                .extension = tawc_dri_request,
+                .length = 0,
+                .evtype = TAWCDRIEventBufferRelease,
+                .eid = event->eid,
+                .window = window_id,
+                .serial = serial,
+            };
+            WriteEventsToClient(event->client, 1, (xEvent *) &br);
+        }
+    }
+}
+
+static void _X_COLD
+tawc_dri_event_swap(xGenericEvent *from, xGenericEvent *to)
+{
+    *to = *from;
+    swaps(&to->sequenceNumber);
+    swapl(&to->length);
+    swaps(&to->evtype);
+    switch (from->evtype) {
+    case TAWCDRIEventConfigureNotify: {
+        xTAWCDRIConfigureNotify *cn = (xTAWCDRIConfigureNotify *) to;
+        swapl(&cn->eid);
+        swapl(&cn->window);
+        swapl(&cn->width);
+        swapl(&cn->height);
+        break;
+    }
+    case TAWCDRIEventBufferRelease: {
+        xTAWCDRIBufferRelease *br = (xTAWCDRIBufferRelease *) to;
+        swapl(&br->eid);
+        swapl(&br->window);
+        swapl(&br->serial);
+        break;
+    }
+    }
+}
+
+static int
+ProcTAWCDRIQueryVersion(ClientPtr client)
+{
+    REQUEST(xTAWCDRIQueryVersionReq);
+    xTAWCDRIQueryVersionReply rep;
+
+    REQUEST_SIZE_MATCH(xTAWCDRIQueryVersionReq);
+
+    rep = (xTAWCDRIQueryVersionReply) {
+        .type = X_Reply,
+        .sequenceNumber = client->sequence,
+        .length = 0,
+        .majorVersion = TAWCDRI_MAJOR,
+        .minorVersion = TAWCDRI_MINOR,
+    };
+    if (client->swapped) {
+        swaps(&rep.sequenceNumber);
+        swapl(&rep.length);
+        swapl(&rep.majorVersion);
+        swapl(&rep.minorVersion);
+    }
+    WriteToClient(client, sizeof(rep), &rep);
+    return Success;
+}
+
+static int
+ProcTAWCDRIPresentBuffer(ClientPtr client)
+{
+    REQUEST(xTAWCDRIPresentBufferReq);
+    WindowPtr window;
+    int *fds = NULL;
+    int32_t *ints_buf = NULL;
+    int rc;
+    uint64_t usage;
+    int num_fds, num_ints;
+    size_t total, ints_sz, ints_offset;
+    uint32_t serial;
+
+    /* Wire layout: fixed header + numInts u32s; length is in 4-byte
+     * words. Two header shapes exist: v0.3 (40 bytes, trailing serial)
+     * and v0.2 (36 bytes, no serial). numFds/numInts sit inside the
+     * common 36-byte prefix, so the total length discriminates the
+     * shapes exactly (they differ by one word). */
+    if (client->req_len < (sz_xTAWCDRIPresentBufferReqV02 >> 2))
+        return BadLength;
+
+    num_fds  = stuff->numFds;
+    num_ints = stuff->numInts;
+
+    /* Reasonable caps so a malformed client can't OOM us. AOSP gralloc4
+     * private_handle_t maxes at numFds=2 (gralloc fd + sync fd) and
+     * numInts ~ 32 in practice; 16/256 is a generous upper bound. */
+    if (num_fds < 0 || num_fds > 16 || num_ints < 0 || num_ints > 256) {
+        client->errorValue = num_fds;
+        return BadValue;
+    }
+
+    total   = (size_t)stuff->length * 4;
+    ints_sz = (size_t)num_ints * sizeof(int32_t);
+    if (total == sz_xTAWCDRIPresentBufferReq + ints_sz) {
+        ints_offset = sz_xTAWCDRIPresentBufferReq;
+        serial = stuff->serial;
+    }
+    else if (total == sz_xTAWCDRIPresentBufferReqV02 + ints_sz) {
+        ints_offset = sz_xTAWCDRIPresentBufferReqV02;
+        serial = 0;
+    }
+    else
+        return BadLength;
+
+    /* DIX requires every dispatch that pulls fds to declare the count up
+     * front; otherwise libxtrans drops the SCM_RIGHTS-attached fds and
+     * ReadFdFromClient returns -1. Same shape as DRI3's
+     * proc_dri3_pixmap_from_buffers. */
+    SetReqFds(client, num_fds);
+
+    rc = dixLookupWindow(&window, stuff->window, client, DixWriteAccess);
+    if (rc != Success)
+        return rc;
+
+    /* Inline ints sit immediately after the request header. The X
+     * server's request buffer is byte-swapped on the wire-side if
+     * client->swapped, so for a swapped client we have to swap each
+     * int here. The values are opaque gralloc bookkeeping (BPP, layer
+     * count, vendor flags) — the client's libnativewindow generates
+     * them on the same architecture as the server in our setup, so
+     * cross-endian is paranoia rather than a real path. */
+    if (num_ints > 0) {
+        const int32_t *raw =
+            (const int32_t *)((const char *)stuff + ints_offset);
+        ints_buf = malloc(num_ints * sizeof(int32_t));
+        if (!ints_buf)
+            return BadAlloc;
+        for (int i = 0; i < num_ints; i++) {
+            int32_t v = raw[i];
+            if (client->swapped) {
+                CARD32 tmp = (CARD32)v;
+                swapl(&tmp);
+                v = (int32_t)tmp;
+            }
+            ints_buf[i] = v;
+        }
+    }
+
+    if (num_fds > 0) {
+        fds = malloc(num_fds * sizeof(int));
+        if (!fds) {
+            free(ints_buf);
+            return BadAlloc;
+        }
+        for (int i = 0; i < num_fds; i++) {
+            fds[i] = ReadFdFromClient(client);
+            if (fds[i] < 0) {
+                while (--i >= 0) close(fds[i]);
+                free(fds);
+                free(ints_buf);
+                return BadValue;
+            }
+        }
+    }
+
+    usage = ((uint64_t)stuff->usageHi << 32) | (uint64_t)stuff->usageLo;
+
+    rc = xwl_tawc_present_native_handle(window, fds, num_fds,
+                                        ints_buf, num_ints,
+                                        (int)stuff->width,
+                                        (int)stuff->height,
+                                        (int)stuff->stride,
+                                        (int)stuff->format,
+                                        usage,
+                                        (uint32_t)client->clientAsMask,
+                                        serial);
+    /* xwl_tawc_present_native_handle owns + closes the fds on both
+     * success (handed to the AHB) and failure (closed during cleanup). */
+    free(fds);
+    free(ints_buf);
+    return rc;
+}
+
+static int
+ProcTAWCDRISelectInput(ClientPtr client)
+{
+    REQUEST(xTAWCDRISelectInputReq);
+    WindowPtr window;
+    tawc_dri_event_rec *event;
+    int rc;
+
+    REQUEST_SIZE_MATCH(xTAWCDRISelectInputReq);
+
+    if (stuff->eventMask & ~TAWCDRIAllEventMasks) {
+        client->errorValue = stuff->eventMask;
+        return BadValue;
+    }
+
+    rc = dixLookupWindow(&window, stuff->window, client, DixGetAttrAccess);
+    if (rc != Success)
+        return rc;
+
+    /* Modifying an existing selection? (Same shape as Present.) */
+    rc = dixLookupResourceByType((void **) &event, stuff->eid,
+                                 tawc_dri_event_type, client,
+                                 DixWriteAccess);
+    if (rc == Success) {
+        if (event->window != window || event->client != client)
+            return BadMatch;
+        if (stuff->eventMask) {
+            event->mask = stuff->eventMask;
+            if (stuff->eventMask & TAWCDRIConfigureNotifyMask)
+                tawc_dri_send_configure_notify_to(event,
+                                                  window->drawable.width,
+                                                  window->drawable.height);
+        }
+        else
+            FreeResource(stuff->eid, RT_NONE);
+        return Success;
+    }
+    if (rc != BadValue)
+        return rc;
+
+    if (stuff->eventMask == 0)
+        return Success;
+
+    LEGAL_NEW_RESOURCE(stuff->eid, client);
+
+    event = calloc(1, sizeof(*event));
+    if (!event)
+        return BadAlloc;
+    event->client = client;
+    event->window = window;
+    event->eid = stuff->eid;
+    event->mask = stuff->eventMask;
+    event->next = tawc_dri_event_list;
+    tawc_dri_event_list = event;
+
+    if (!AddResource(event->eid, tawc_dri_event_type, (void *) event))
+        return BadAlloc;
+
+    /* First selection on this window: attach the cleanup marker so
+     * window destruction frees the selections. The marker shares the
+     * window's XID; dix frees every resource under that XID when the
+     * window dies. */
+    {
+        void *marker;
+        rc = dixLookupResourceByType(&marker, window->drawable.id,
+                                     tawc_dri_window_type, serverClient,
+                                     DixReadAccess);
+        if (rc != Success &&
+            !AddResource(window->drawable.id, tawc_dri_window_type,
+                         (void *) window)) {
+            /* A failed AddResource already ran tawc_dri_free_window,
+             * which freed `event` via its eid resource — use the
+             * request's copy of the XID (a second FreeResource on an
+             * already-freed XID is a harmless no-op). */
+            FreeResource(stuff->eid, RT_NONE);
+            return BadAlloc;
+        }
+    }
+
+    /* Deliver the window's current size right away: a WM resize that
+     * landed between window creation and this SelectInput would
+     * otherwise be missed forever (this exact race is how eglx11-test
+     * accidentally survived the black-window bug while es2gears_x11
+     * didn't). */
+    if (stuff->eventMask & TAWCDRIConfigureNotifyMask)
+        tawc_dri_send_configure_notify_to(event,
+                                          window->drawable.width,
+                                          window->drawable.height);
+
+    return Success;
+}
+
+static int
+ProcTAWCDRIDispatch(ClientPtr client)
+{
+    REQUEST(xReq);
+    switch (stuff->data) {
+    case X_TAWCDRIQueryVersion:
+        return ProcTAWCDRIQueryVersion(client);
+    case X_TAWCDRIPresentBuffer:
+        return ProcTAWCDRIPresentBuffer(client);
+    case X_TAWCDRISelectInput:
+        return ProcTAWCDRISelectInput(client);
+    default:
+        return BadRequest;
+    }
+}
+
+static int _X_COLD
+SProcTAWCDRIQueryVersion(ClientPtr client)
+{
+    REQUEST(xTAWCDRIQueryVersionReq);
+    swaps(&stuff->length);
+    REQUEST_SIZE_MATCH(xTAWCDRIQueryVersionReq);
+    swapl(&stuff->majorVersion);
+    swapl(&stuff->minorVersion);
+    return ProcTAWCDRIQueryVersion(client);
+}
+
+static int _X_COLD
+SProcTAWCDRIPresentBuffer(ClientPtr client)
+{
+    REQUEST(xTAWCDRIPresentBufferReq);
+    swaps(&stuff->length);
+    if (client->req_len < (sz_xTAWCDRIPresentBufferReqV02 >> 2))
+        return BadLength;
+    swapl(&stuff->window);
+    swaps(&stuff->numFds);
+    swaps(&stuff->numInts);
+    swapl(&stuff->width);
+    swapl(&stuff->height);
+    swapl(&stuff->stride);
+    swapl(&stuff->format);
+    swapl(&stuff->usageLo);
+    swapl(&stuff->usageHi);
+    /* Only the v0.3 shape has the trailing serial; for a v0.2 request
+     * that word is the first inline int, which the lazy per-int swap
+     * inside ProcTAWCDRIPresentBuffer handles (double-swapping it here
+     * would corrupt it). */
+    if ((size_t)stuff->length * 4 >=
+            sz_xTAWCDRIPresentBufferReq +
+            (size_t)stuff->numInts * sizeof(int32_t))
+        swapl(&stuff->serial);
+    return ProcTAWCDRIPresentBuffer(client);
+}
+
+static int _X_COLD
+SProcTAWCDRISelectInput(ClientPtr client)
+{
+    REQUEST(xTAWCDRISelectInputReq);
+    swaps(&stuff->length);
+    REQUEST_SIZE_MATCH(xTAWCDRISelectInputReq);
+    swapl(&stuff->eid);
+    swapl(&stuff->window);
+    swapl(&stuff->eventMask);
+    return ProcTAWCDRISelectInput(client);
+}
+
+static int _X_COLD
+SProcTAWCDRIDispatch(ClientPtr client)
+{
+    REQUEST(xReq);
+    switch (stuff->data) {
+    case X_TAWCDRIQueryVersion:
+        return SProcTAWCDRIQueryVersion(client);
+    case X_TAWCDRIPresentBuffer:
+        return SProcTAWCDRIPresentBuffer(client);
+    case X_TAWCDRISelectInput:
+        return SProcTAWCDRISelectInput(client);
+    default:
+        return BadRequest;
+    }
+}
+
+void
+tawc_dri_extension_init(void)
+{
+    ExtensionEntry *extEntry;
+    int i;
+
+    /* Per-generation state: resources were freed by the reset, but the
+     * statics survive. */
+    tawc_dri_event_list = NULL;
+
+    tawc_dri_event_type =
+        CreateNewResourceType(tawc_dri_free_event, "TAWCDRIEvent");
+    tawc_dri_window_type =
+        CreateNewResourceType(tawc_dri_free_window, "TAWCDRIWindow");
+    if (!tawc_dri_event_type || !tawc_dri_window_type)
+        FatalError("TAWC-DRI: failed to create resource types\n");
+
+    extEntry = AddExtension(TAWCDRI_NAME,
+                            TAWCDRINumberEvents,
+                            TAWCDRINumberErrors,
+                            ProcTAWCDRIDispatch,
+                            SProcTAWCDRIDispatch,
+                            NULL,
+                            StandardMinorOpcode);
+    if (!extEntry)
+        FatalError("TAWC-DRI: AddExtension failed\n");
+    tawc_dri_request = extEntry->base;
+    GERegisterExtension(tawc_dri_request, tawc_dri_event_swap);
+
+    /* Extensions initialize after InitOutput, so the screens exist and
+     * we can hook ConfigNotify to hear about window resizes. */
+    for (i = 0; i < screenInfo.numScreens; i++) {
+        ScreenPtr screen = screenInfo.screens[i];
+        tawc_dri_wrapped_config_notify[i] = screen->ConfigNotify;
+        screen->ConfigNotify = tawc_dri_config_notify;
+    }
+}
diff --git a/Xext/tawcdriproto.h b/Xext/tawcdriproto.h
new file mode 100644
index 000000000..2e9917b13
--- /dev/null
+++ b/Xext/tawcdriproto.h
@@ -0,0 +1,155 @@
+/*
+ * SPDX-License-Identifier: MIT
+ *
+ * TAWC-DRI: AHB-shipping X11 protocol extension. AHB equivalent of
+ * DRI3. The Phase 1 stub had a fabricated buffer-id; Phase 2 step 3
+ * carries a real Android native_handle_t over the wire — `numInts` ints
+ * inline in the request body and `numFds` fds out-of-band via X11's
+ * SCM_RIGHTS-style FD passing (the same mechanism DRI3's PixmapFromBuffer
+ * uses; ReadFdFromClient on the server side, xcb_send_request_with_fds
+ * on the client). The server then rebuilds an AHardwareBuffer via
+ * AHardwareBuffer_createFromHandle, exactly matching what
+ * compositor/native/wlegl_import.c does on the compositor side.
+ *
+ * See notes/xwayland.md.
+ */
+
+#ifndef _TAWCDRIPROTO_H_
+#define _TAWCDRIPROTO_H_
+
+#include <X11/X.h>
+#include <X11/Xmd.h>
+#include <X11/Xproto.h>
+
+#define TAWCDRI_NAME "TAWC-DRI"
+#define TAWCDRI_MAJOR 0
+#define TAWCDRI_MINOR 3
+
+#define X_TAWCDRIQueryVersion  0
+#define X_TAWCDRIPresentBuffer 1
+#define X_TAWCDRISelectInput   2
+
+/* XGE generic events (response_type 35), not core events. */
+#define TAWCDRINumberErrors 0
+#define TAWCDRINumberEvents 0
+
+#define TAWCDRIEventConfigureNotify 0
+#define TAWCDRIEventBufferRelease   1
+
+#define TAWCDRIConfigureNotifyMask (1 << 0)
+#define TAWCDRIBufferReleaseMask   (1 << 1)
+#define TAWCDRIAllEventMasks \
+    (TAWCDRIConfigureNotifyMask | TAWCDRIBufferReleaseMask)
+
+typedef struct {
+    CARD8  reqType;
+    CARD8  tawcReqType;
+    CARD16 length B16;
+    CARD32 majorVersion B32;
+    CARD32 minorVersion B32;
+} xTAWCDRIQueryVersionReq;
+#define sz_xTAWCDRIQueryVersionReq 12
+
+typedef struct {
+    BYTE   type;
+    BYTE   pad1;
+    CARD16 sequenceNumber B16;
+    CARD32 length B32;
+    CARD32 majorVersion B32;
+    CARD32 minorVersion B32;
+    CARD32 pad2;
+    CARD32 pad3;
+    CARD32 pad4;
+    CARD32 pad5;
+} xTAWCDRIQueryVersionReply;
+#define sz_xTAWCDRIQueryVersionReply 32
+
+/* PresentBuffer ships an entire AHB-backed pixmap: the native_handle
+ * payload (numFds + numInts and the inline ints) plus the AHB descriptor
+ * (width, height, stride, format, usage). FDs follow out-of-band via
+ * X11 FD passing; the server reads them with ReadFdFromClient(). The
+ * `numInts` inline u32s start immediately after this struct, padded
+ * up to 4-byte alignment by libxtrans. usage is split into lo/hi u32
+ * because Xproto wire types are 32-bit.
+ *
+ * The buffer is bound to `window`'s X11 toplevel and committed on its
+ * wl_surface; the request is void (no reply, errors arrive
+ * asynchronously).
+ *
+ * v0.3: `serial` is a client-chosen cookie echoed back in the
+ * TAWCDRIBufferRelease event when the compositor releases the
+ * wl_buffer this present created. */
+typedef struct {
+    CARD8  reqType;
+    CARD8  tawcReqType;
+    CARD16 length B16;
+    CARD32 window B32;
+    CARD16 numFds B16;
+    CARD16 numInts B16;
+    CARD32 width B32;
+    CARD32 height B32;
+    CARD32 stride B32;
+    CARD32 format B32;
+    CARD32 usageLo B32;
+    CARD32 usageHi B32;
+    CARD32 serial B32;
+} xTAWCDRIPresentBufferReq;
+#define sz_xTAWCDRIPresentBufferReq 40
+/* The v0.2 request is the same struct without the trailing serial.
+ * The server accepts both shapes (discriminated by total request
+ * length, which is unambiguous once numInts is known) so pre-0.3
+ * clients keep working against a v0.3 server. */
+#define sz_xTAWCDRIPresentBufferReqV02 36
+
+/* v0.3: select TAWC-DRI events on `window`. `eid` is a client-allocated
+ * XID (xcb_generate_id) used as the routing key for libxcb's special
+ * event queues — same role as the Present extension's event id. An
+ * eventMask of 0 deletes the selection. Selecting ConfigureNotify
+ * immediately emits one event carrying the window's current size, so
+ * a resize that landed before the selection is never missed. */
+typedef struct {
+    CARD8  reqType;
+    CARD8  tawcReqType;
+    CARD16 length B16;
+    CARD32 eid B32;
+    CARD32 window B32;
+    CARD32 eventMask B32;
+} xTAWCDRISelectInputReq;
+#define sz_xTAWCDRISelectInputReq 16
+
+/* Events are XGE generic events. Field layout deliberately matches the
+ * Present extension's events — evtype at bytes 8-9, eid at bytes 12-15 —
+ * because libxcb's special-event matching (xcb_in.c, xcb_ge_special_event_t)
+ * reads the routing eid at that fixed offset. */
+
+typedef struct {
+    CARD8  type;                /* GenericEvent */
+    CARD8  extension;           /* TAWC-DRI major opcode */
+    CARD16 sequenceNumber B16;
+    CARD32 length B32;          /* 0 — no payload past 32 bytes */
+    CARD16 evtype B16;          /* TAWCDRIEventConfigureNotify */
+    CARD16 pad1 B16;
+    CARD32 eid B32;
+    CARD32 window B32;
+    CARD32 width B32;
+    CARD32 height B32;
+    CARD32 pad2 B32;
+} xTAWCDRIConfigureNotify;
+#define sz_xTAWCDRIConfigureNotify 32
+
+typedef struct {
+    CARD8  type;                /* GenericEvent */
+    CARD8  extension;           /* TAWC-DRI major opcode */
+    CARD16 sequenceNumber B16;
+    CARD32 length B32;          /* 0 */
+    CARD16 evtype B16;          /* TAWCDRIEventBufferRelease */
+    CARD16 pad1 B16;
+    CARD32 eid B32;
+    CARD32 window B32;
+    CARD32 serial B32;          /* echoed from PresentBuffer */
+    CARD32 pad2 B32;
+    CARD32 pad3 B32;
+} xTAWCDRIBufferRelease;
+#define sz_xTAWCDRIBufferRelease 32
+
+#endif /* _TAWCDRIPROTO_H_ */
diff --git a/hw/xwayland/android_wlegl.xml b/hw/xwayland/android_wlegl.xml
new file mode 100644
index 000000000..24ef7805a
--- /dev/null
+++ b/hw/xwayland/android_wlegl.xml
@@ -0,0 +1,127 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<protocol name="android">
+
+  <copyright>
+    Copyright © 2012 Collabora, Ltd.
+
+    Permission to use, copy, modify, distribute, and sell this
+    software and its documentation for any purpose is hereby granted
+    without fee, provided that the above copyright notice appear in
+    all copies and that both that copyright notice and this permission
+    notice appear in supporting documentation, and that the name of
+    the copyright holders not be used in advertising or publicity
+    pertaining to distribution of the software without specific,
+    written prior permission.  The copyright holders make no
+    representations about the suitability of this software for any
+    purpose.  It is provided "as is" without express or implied
+    warranty.
+
+    THE COPYRIGHT HOLDERS DISCLAIM ALL WARRANTIES WITH REGARD TO THIS
+    SOFTWARE, INCLUDING ALL IMPLIED WARRANTIES OF MERCHANTABILITY AND
+    FITNESS, IN NO EVENT SHALL THE COPYRIGHT HOLDERS BE LIABLE FOR ANY
+    SPECIAL, INDIRECT OR CONSEQUENTIAL DAMAGES OR ANY DAMAGES
+    WHATSOEVER RESULTING FROM LOSS OF USE, DATA OR PROFITS, WHETHER IN
+    AN ACTION OF CONTRACT, NEGLIGENCE OR OTHER TORTIOUS ACTION,
+    ARISING OUT OF OR IN CONNECTION WITH THE USE OR PERFORMANCE OF
+    THIS SOFTWARE.
+  </copyright>
+
+  <interface name="android_wlegl" version="2">
+    <description summary="Android EGL graphics buffer support">
+      Interface used in the Android wrapper libEGL to share
+      graphics buffers between the server and the client.
+    </description>
+
+    <enum name="error">
+      <entry name="bad_handle" value="0" />
+      <entry name="bad_value" value="1" />
+    </enum>
+
+    <request name="create_handle">
+      <description summary="Create an Android native_handle_t object">
+        This creator method initialises the native_handle_t object
+        with everything except the file descriptors, which have to be
+        submitted separately.
+      </description>
+
+      <arg name="id" type="new_id" interface="android_wlegl_handle" />
+      <arg name="num_fds" type="int" />
+      <arg name="ints" type="array" summary="an array of int32_t" />
+    </request>
+
+    <request name="create_buffer">
+      <description summary="Create a wl_buffer from the native handle">
+        Pass the Android native_handle_t to the server and attach it
+        to the new wl_buffer object.
+
+        The android_wlegl_handle object must be destroyed immediately
+        after this request.
+      </description>
+
+      <arg name="id" type="new_id" interface="wl_buffer" />
+      <arg name="width" type="int" />
+      <arg name="height" type="int" />
+      <arg name="stride" type="int" />
+      <arg name="format" type="int" />
+      <arg name="usage" type="int" />
+      <arg name="native_handle" type="object" interface="android_wlegl_handle" />
+    </request>
+
+
+    <request name="get_server_buffer_handle" since="2">
+        <arg name="id" type="new_id" interface="android_wlegl_server_buffer_handle"/>
+        <arg name="width" type="int"/>
+        <arg name="height" type="int"/>
+        <arg name="format" type="int"/>
+        <arg name="usage" type="int"/>
+    </request>
+
+  </interface>
+
+  <interface name="android_wlegl_handle" version="1">
+    <description summary="An Android native_handle_t object">
+      The Android native_handle_t is a semi-opaque object, that
+      contains an EGL implementation specific number of int32
+      values and file descriptors.
+
+      We cannot send a variable size array of file descriptors
+      over the Wayland protocol, so we send them one by one.
+    </description>
+
+    <enum name="error">
+      <entry name="too_many_fds" value="0" />
+    </enum>
+
+    <request name="add_fd">
+      <arg name="fd" type="fd" />
+    </request>
+
+    <request name="destroy" type="destructor" />
+
+  </interface>
+
+
+  <interface name="android_wlegl_server_buffer_handle" version="1">
+      <description summary="a server allocated buffer">
+          On creation a server_side_buffer object will immediately send
+          the "buffer_fd" and "buffer_ints" events needed by the client to
+          reference the gralloc buffer, followed by the "buffer" event carrying
+          the wl_buffer object.
+      </description>
+      <event name="buffer_fd">
+          <arg name="fd" type="fd"/>
+      </event>
+      <event name="buffer_ints">
+          <arg name="ints" type="array"/>
+      </event>
+      <event name="buffer">
+          <description summary="the wl_buffer">
+              This event will be sent after the ints and all the fds have been sent
+          </description>
+          <arg name="buffer" type="new_id" interface="wl_buffer"/>
+          <arg name="format" type="int"/>
+          <arg name="stride" type="int"/>
+      </event>
+  </interface>
+
+</protocol>
diff --git a/hw/xwayland/meson.build b/hw/xwayland/meson.build
index 9fff6445d..950c9a57d 100644
--- a/hw/xwayland/meson.build
+++ b/hw/xwayland/meson.build
@@ -127,6 +127,17 @@ if build_xwayland_glamor
     xwayland_glamor += glamor
 endif
 
+if build_tawc
+    srcs += [
+        'xwayland-tawc.c',
+        'xwayland-tawc.h',
+        'xwayland-tawc-libnativewindow.c',
+        'xwayland-tawc-libnativewindow.h',
+    ]
+    srcs += client_header.process('android_wlegl.xml')
+    srcs += code.process('android_wlegl.xml')
+endif
+
 wayland_inc = [ inc, ]
 if build_glx
     wayland_inc += glx_inc
diff --git a/hw/xwayland/xwayland-screen.c b/hw/xwayland/xwayland-screen.c
index c83adda9f..383947d23 100644
--- a/hw/xwayland/xwayland-screen.c
+++ b/hw/xwayland/xwayland-screen.c
@@ -71,6 +71,11 @@
 #include "tearing-control-v1-client-protocol.h"
 #include "fractional-scale-v1-client-protocol.h"
 
+#ifdef TAWC
+#include "android_wlegl-client-protocol.h"
+#include "xwayland-tawc.h"
+#endif
+
 static DevPrivateKeyRec xwl_screen_private_key;
 static DevPrivateKeyRec xwl_client_private_key;
 
@@ -542,6 +547,16 @@ registry_global(void *data, struct wl_registry *registry, uint32_t id,
         xwl_screen->fractional_scale_manager =
             wl_registry_bind(registry, id, &wp_fractional_scale_manager_v1_interface, 1);
     }
+#ifdef TAWC
+    else if (strcmp(interface, android_wlegl_interface.name) == 0) {
+        /* Bind v2: matches the compositor's advertised version. We only
+         * use v1 requests (create_handle / create_buffer); v2 adds
+         * get_server_buffer_handle, which the compositor rejects. */
+        uint32_t v = version < 2 ? version : 2;
+        xwl_screen->tawc_wlegl =
+            wl_registry_bind(registry, id, &android_wlegl_interface, v);
+    }
+#endif
 #ifdef XWL_HAS_GLAMOR
     else if (xwl_screen->glamor) {
         xwl_glamor_init_wl_registry(xwl_screen, registry, id, interface,
@@ -1030,7 +1045,6 @@ xwl_screen_init(ScreenPtr pScreen, int argc, char **argv)
                              &registry_listener, xwl_screen);
     xwl_screen_roundtrip(xwl_screen);
 
-
     if (xwl_screen->fullscreen && xwl_screen->rootless) {
         ErrorF("error, cannot set fullscreen when running rootless\n");
         return FALSE;
diff --git a/hw/xwayland/xwayland-screen.h b/hw/xwayland/xwayland-screen.h
index 9751bd25d..8aadce761 100644
--- a/hw/xwayland/xwayland-screen.h
+++ b/hw/xwayland/xwayland-screen.h
@@ -44,6 +44,10 @@
 #include <libdecor.h>
 #endif
 
+#ifdef TAWC
+struct android_wlegl;
+#endif
+
 struct xwl_screen {
     double width;
     double height;
@@ -150,6 +154,12 @@ struct xwl_screen {
     const char *output_name;
 
     uint32_t present_capabilities;
+
+#ifdef TAWC
+    /* android_wlegl global, used to ship AHB-backed buffers from this
+     * X server to the compositor. See hw/xwayland/xwayland-tawc.c. */
+    struct android_wlegl *tawc_wlegl;
+#endif
 };
 
 /* Apps which use randr/vidmode to change the mode when going fullscreen,
diff --git a/hw/xwayland/xwayland-tawc-libnativewindow.c b/hw/xwayland/xwayland-tawc-libnativewindow.c
new file mode 100644
index 000000000..993feac9f
--- /dev/null
+++ b/hw/xwayland/xwayland-tawc-libnativewindow.c
@@ -0,0 +1,117 @@
+/*
+ * SPDX-License-Identifier: MIT
+ *
+ * libnativewindow.so dlopen wrapper. Bionic-only; we link against the
+ * NDK already, but going through dlopen keeps libnativewindow out of
+ * Xwayland's DT_NEEDED so a build that's never run on a real Android
+ * device (e.g. the unit-test harness) doesn't fail to load.
+ *
+ * AHardwareBuffer_getNativeHandle is "private" in NDK header parlance
+ * (declared in private/android/AHardwareBufferHelpers.h) but is in
+ * libnativewindow.so's exported map under LIBNATIVEWINDOW_PLATFORM —
+ * dlsym sees it. We need it because android_wlegl ships native_handle
+ * payload (numFds, numInts, fds, ints), not opaque AHB handles, so
+ * we have to crack the AHB to get the native_handle ABI back.
+ *
+ * See notes/xwayland.md.
+ */
+
+#ifdef HAVE_DIX_CONFIG_H
+#include <dix-config.h>
+#endif
+
+#include "xwayland-tawc-libnativewindow.h"
+
+#include <dlfcn.h>
+#include <pthread.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <string.h>
+
+#include "os.h"
+
+/* Function-pointer typedefs — match libnativewindow's NDK shape. */
+typedef void (*pfn_release_t)(struct AHardwareBuffer *);
+typedef const struct tawc_native_handle *
+             (*pfn_get_handle_t)(const struct AHardwareBuffer *);
+typedef int  (*pfn_from_handle_t)(const struct tawc_ahb_desc *,
+                                  const struct tawc_native_handle *,
+                                  int32_t method,
+                                  struct AHardwareBuffer **);
+
+/* From AOSP frameworks/native/libs/nativewindow/include/private/android/
+ * AHardwareBufferHelpers.h. METHOD_REGISTER means the AHB takes ownership
+ * of the handle's fds. */
+#define TAWC_AHB_METHOD_REGISTER 2
+
+static pfn_release_t     g_release;
+static pfn_get_handle_t  g_get_handle;
+static pfn_from_handle_t g_from_handle;
+static int               g_loaded;
+
+static pthread_once_t g_once = PTHREAD_ONCE_INIT;
+
+static void
+load_once(void)
+{
+    void *lib = dlopen("libnativewindow.so", RTLD_NOW | RTLD_GLOBAL);
+    if (!lib) {
+        ErrorF("xwl_tawc: dlopen(libnativewindow.so) failed: %s\n",
+               dlerror());
+        return;
+    }
+    g_release     = (pfn_release_t)    dlsym(lib, "AHardwareBuffer_release");
+    g_get_handle  = (pfn_get_handle_t) dlsym(lib, "AHardwareBuffer_getNativeHandle");
+    g_from_handle = (pfn_from_handle_t) dlsym(lib, "AHardwareBuffer_createFromHandle");
+
+    if (!g_release || !g_get_handle || !g_from_handle) {
+        ErrorF("xwl_tawc: dlsym AHardwareBuffer_* failed "
+               "(release=%p get_handle=%p from_handle=%p)\n",
+               (void *)g_release, (void *)g_get_handle, (void *)g_from_handle);
+        return;
+    }
+    g_loaded = 1;
+}
+
+int
+xwl_tawc_libnativewindow_load(void)
+{
+    pthread_once(&g_once, load_once);
+    return g_loaded;
+}
+
+void
+xwl_tawc_ahb_release(struct AHardwareBuffer *ahb)
+{
+    if (!ahb || !xwl_tawc_libnativewindow_load())
+        return;
+    g_release(ahb);
+}
+
+const struct tawc_native_handle *
+xwl_tawc_ahb_get_native_handle(struct AHardwareBuffer *ahb)
+{
+    if (!ahb || !xwl_tawc_libnativewindow_load())
+        return NULL;
+    return g_get_handle(ahb);
+}
+
+int
+xwl_tawc_ahb_create_from_handle(const struct tawc_ahb_desc *desc,
+                                const struct tawc_native_handle *handle,
+                                struct AHardwareBuffer **out)
+{
+    if (!xwl_tawc_libnativewindow_load() || !desc || !handle || !out)
+        return -1;
+    *out = NULL;
+    int rc = g_from_handle(desc, handle, TAWC_AHB_METHOD_REGISTER, out);
+    if (rc != 0 || !*out) {
+        ErrorF("xwl_tawc: AHardwareBuffer_createFromHandle failed rc=%d "
+               "(%ux%u fmt=%u stride=%u usage=0x%llx numFds=%d numInts=%d)\n",
+               rc, desc->width, desc->height, desc->format, desc->stride,
+               (unsigned long long)desc->usage,
+               handle->numFds, handle->numInts);
+        return rc != 0 ? rc : -1;
+    }
+    return 0;
+}
diff --git a/hw/xwayland/xwayland-tawc-libnativewindow.h b/hw/xwayland/xwayland-tawc-libnativewindow.h
new file mode 100644
index 000000000..763f23827
--- /dev/null
+++ b/hw/xwayland/xwayland-tawc-libnativewindow.h
@@ -0,0 +1,66 @@
+/*
+ * SPDX-License-Identifier: MIT
+ *
+ * Thin wrapper around libnativewindow.so — bionic NDK API for AHB
+ * allocation, plus the (exported-but-private) AHardwareBuffer_getNativeHandle
+ * symbol we need to ship a buffer over android_wlegl. See notes/xwayland.md.
+ */
+
+#ifndef XWAYLAND_TAWC_LIBNATIVEWINDOW_H
+#define XWAYLAND_TAWC_LIBNATIVEWINDOW_H
+
+#include <stdint.h>
+
+/* Opaque to callers; we don't pull in <android/hardware_buffer.h>
+ * because Xwayland's includes don't establish an Android NDK sysroot
+ * for every TU. Pointers only. */
+struct AHardwareBuffer;
+
+/* Mirror of cutils' native_handle_t. Stable ABI. */
+struct tawc_native_handle {
+    int version;   /* sizeof(native_handle_t) = 12 */
+    int numFds;
+    int numInts;
+    int data[];    /* numFds fds, then numInts ints */
+};
+
+/* AHardwareBuffer_Desc subset (matches the NDK layout). */
+struct tawc_ahb_desc {
+    uint32_t width;
+    uint32_t height;
+    uint32_t layers;
+    uint32_t format;
+    uint64_t usage;
+    uint32_t stride;
+    uint32_t rfu0;
+    uint64_t rfu1;
+};
+
+/* Format constants we actually use, mirrored so callers don't need
+ * <android/hardware_buffer.h>. */
+#define TAWC_AHB_FORMAT_R8G8B8A8_UNORM      1
+#define TAWC_AHB_FORMAT_R8G8B8X8_UNORM      2
+#define TAWC_AHB_FORMAT_B8G8R8A8_UNORM      5
+
+/* Resolve symbols from libnativewindow.so. Returns 1 on success, 0 on
+ * failure. Idempotent; safe to call repeatedly. */
+int xwl_tawc_libnativewindow_load(void);
+
+/* AHardwareBuffer_release. */
+void xwl_tawc_ahb_release(struct AHardwareBuffer *ahb);
+
+/* AHardwareBuffer_createFromHandle: rebuild an AHB from a wire-format
+ * native_handle (numFds, numInts, fds, ints). Mirrors what
+ * compositor/native/wlegl_import.c does on the compositor side. The
+ * AHB takes ownership of the fds in `handle->data[0..numFds]` on
+ * success; on failure the caller still owns them. Returns 0 on success. */
+int xwl_tawc_ahb_create_from_handle(const struct tawc_ahb_desc *desc,
+                                    const struct tawc_native_handle *handle,
+                                    struct AHardwareBuffer **out);
+
+/* Borrow the AHB's underlying native_handle_t. Lifetime bound to the
+ * AHB; do not free. The pointer becomes invalid after _release(). */
+const struct tawc_native_handle *
+xwl_tawc_ahb_get_native_handle(struct AHardwareBuffer *ahb);
+
+#endif /* XWAYLAND_TAWC_LIBNATIVEWINDOW_H */
diff --git a/hw/xwayland/xwayland-tawc.c b/hw/xwayland/xwayland-tawc.c
new file mode 100644
index 000000000..c8cf0005b
--- /dev/null
+++ b/hw/xwayland/xwayland-tawc.c
@@ -0,0 +1,410 @@
+/*
+ * SPDX-License-Identifier: MIT
+ *
+ * TAWC AHB buffer transport for Xwayland.
+ *
+ * The protocol shape we send is identical to libhybris's wayland
+ * platform plugin (libhybris/hybris/platforms/wayland/
+ * wayland_window_common.cpp:59), which is what tawc's compositor
+ * accepts from Wayland clients via android_wlegl in v1 mode:
+ *
+ *   1. android_wlegl.create_handle(num_fds, ints[]) -> handle
+ *   2. handle.add_fd(fd) repeated num_fds times
+ *   3. android_wlegl.create_buffer(width, height, stride, format,
+ *                                  usage, handle) -> wl_buffer
+ *   4. handle.destroy()
+ *
+ * The compositor's wlegl_import.c then rebuilds an AHardwareBuffer
+ * from the (fds, ints) pair via AHardwareBuffer_createFromHandle.
+ *
+ * See notes/xwayland.md.
+ */
+
+#ifdef HAVE_DIX_CONFIG_H
+#include <dix-config.h>
+#endif
+
+#include <stdlib.h>
+#include <string.h>
+#include <unistd.h>
+#include <wayland-client.h>
+
+#include <X11/X.h>
+#include "os.h"
+#include "windowstr.h"
+
+#include "xwayland-screen.h"
+#include "xwayland-window.h"
+#include "xwayland-tawc.h"
+#include "xwayland-tawc-libnativewindow.h"
+
+#include "android_wlegl-client-protocol.h"
+
+void
+xwl_tawc_buffer_destroy(struct xwl_tawc_buffer *buf)
+{
+    if (!buf)
+        return;
+    if (buf->buffer)
+        wl_buffer_destroy(buf->buffer);
+    if (buf->ahb)
+        xwl_tawc_ahb_release(buf->ahb);
+    free(buf);
+}
+
+/* Build a wl_buffer from a tawc_native_handle using the v1
+ * create_handle / add_fd / create_buffer sequence. The compositor
+ * speaks v2 of the protocol but rejects get_server_buffer_handle
+ * (server-side allocation), so client-side allocation via these v1
+ * requests is the only path. */
+static struct wl_buffer *
+ship_native_handle(struct android_wlegl *wlegl,
+                   const struct tawc_native_handle *handle,
+                   int width, int height, int stride,
+                   int format, uint64_t usage)
+{
+    struct android_wlegl_handle *wlegl_handle;
+    struct wl_array ints;
+    struct wl_buffer *buffer;
+    int *ints_data;
+
+    wl_array_init(&ints);
+    ints_data = wl_array_add(&ints, handle->numInts * sizeof(int));
+    if (!ints_data) {
+        wl_array_release(&ints);
+        return NULL;
+    }
+    memcpy(ints_data,
+           handle->data + handle->numFds,
+           handle->numInts * sizeof(int));
+
+    wlegl_handle = android_wlegl_create_handle(wlegl, handle->numFds, &ints);
+    wl_array_release(&ints);
+    if (!wlegl_handle)
+        return NULL;
+
+    for (int i = 0; i < handle->numFds; i++)
+        android_wlegl_handle_add_fd(wlegl_handle, handle->data[i]);
+
+    /* usage is uint32_t on the wire — the AHB usage high bits are
+     * gralloc4-internal and the compositor's wlegl_import.c masks
+     * to (uint32_t) anyway. */
+    buffer = android_wlegl_create_buffer(wlegl, width, height, stride,
+                                         format, (uint32_t)usage,
+                                         wlegl_handle);
+    android_wlegl_handle_destroy(wlegl_handle);
+    return buffer;
+}
+
+struct wl_buffer *
+xwl_tawc_buffer_get_wl_buffer(struct xwl_screen *xwl_screen,
+                              struct xwl_tawc_buffer *buf)
+{
+    const struct tawc_native_handle *handle;
+
+    if (!buf || !buf->ahb)
+        return NULL;
+    if (buf->buffer)
+        return buf->buffer;
+    if (!xwl_screen->tawc_wlegl) {
+        ErrorF("xwl_tawc: compositor does not advertise android_wlegl\n");
+        return NULL;
+    }
+
+    handle = xwl_tawc_ahb_get_native_handle(buf->ahb);
+    if (!handle) {
+        ErrorF("xwl_tawc: AHardwareBuffer_getNativeHandle failed\n");
+        return NULL;
+    }
+
+    buf->buffer = ship_native_handle(xwl_screen->tawc_wlegl, handle,
+                                     buf->width, buf->height, buf->stride,
+                                     buf->format, buf->usage);
+    if (!buf->buffer) {
+        ErrorF("xwl_tawc: android_wlegl create_buffer failed\n");
+        return NULL;
+    }
+    return buf->buffer;
+}
+
+/* Step-3 stub: a real impl will look up the xwl_tawc_buffer stashed on
+ * the PixmapPtr by the TAWC-DRI handler and route through the standalone
+ * shipping helpers. For now it returns NULL — TAWC-DRI bypasses the
+ * pixmap router and attaches its wl_buffer directly to the X11 window's
+ * wl_surface, since we don't need to compose AHB-backed pixmaps with the
+ * X server's normal drawing flow yet. */
+struct wl_buffer *
+xwl_tawc_pixmap_get_wl_buffer(PixmapPtr pixmap)
+{
+    (void) pixmap;
+    return NULL;
+}
+
+/* Implemented in Xext/tawc-dri.c (same binary; see the matching
+ * extern note there). Forwards the release to the presenting client's
+ * TAWCDRIBufferRelease selection on the window, if any. */
+extern void tawc_dri_send_buffer_release(uint32_t window_id,
+                                         uint32_t client_mask,
+                                         uint32_t serial);
+
+/* Compositor signals it's done with one of our shipped wl_buffers.
+ * Free the corresponding xwl_tawc_buffer, which destroys the wl_buffer
+ * and releases the AHB (which in turn closes the dup'd fds). Without
+ * this, every present leaks an FD per fd-in-the-handle and the
+ * compositor process trips RLIMIT_NOFILE within seconds of any
+ * sustained-frame client (es2gears_x11, glmark2-es2, …). Also the
+ * v0.3 BufferRelease source: the presenting client learns its AHB is
+ * reusable here, giving real backpressure instead of the old 3-slot
+ * round-robin freshness heuristic. */
+static void
+buffer_release(void *data, struct wl_buffer *buffer)
+{
+    struct xwl_tawc_buffer *buf = data;
+    (void) buffer;
+    tawc_dri_send_buffer_release(buf->window_id, buf->client_mask,
+                                 buf->serial);
+    xwl_tawc_buffer_destroy(buf);
+}
+
+static const struct wl_buffer_listener buffer_release_listener = {
+    .release = buffer_release,
+};
+
+/* ── Frame pacing ──
+ *
+ * Attaching + committing every present the moment it arrives lets an
+ * unthrottled GL client free-run (es2gears_x11 measured 2500+ fps):
+ * each commit makes the compositor release the replaced wl_buffer, so
+ * BufferRelease tracks the client's own present rate and the v0.3
+ * backpressure never engages. Instead, commits are paced by the
+ * compositor's wl_surface.frame callback (sent at display cadence for
+ * visible windows): the first present on an idle surface commits
+ * immediately and requests a callback; presents that arrive while one
+ * is outstanding queue FIFO on the xwl_window, one committed per
+ * callback. Releases then tick at display rate, and a v0.3 client
+ * blocks in dequeueBuffer once all its buffers are busy — clamped to
+ * the display for free. Clients slower than the display never find a
+ * callback outstanding, so they keep the direct-commit latency.
+ *
+ * The compositor only sends frame callbacks for visible windows. For a
+ * hidden window the queue caps at TAWC_MAX_QUEUED and the oldest entry
+ * is dropped *without* a BufferRelease: a v0.3 client then throttles on
+ * its own 500ms force-free fallback instead of free-running against a
+ * window nobody can see (pre-0.3 clients ignore releases either way).
+ * The dropped trio is destroyed, so server-side fds stay bounded. */
+#define TAWC_MAX_QUEUED 8
+
+static void tawc_frame_done(void *data, struct wl_callback *callback,
+                            uint32_t time);
+
+static const struct wl_callback_listener tawc_frame_listener = {
+    .done = tawc_frame_done,
+};
+
+/* Attach + commit now, and request the frame callback that paces the
+ * next commit. buf->buffer must exist (present creates it eagerly). */
+static void
+xwl_tawc_commit(struct xwl_window *xwl_window, struct xwl_tawc_buffer *buf)
+{
+    wl_surface_attach(xwl_window->surface, buf->buffer, 0, 0);
+    wl_surface_damage(xwl_window->surface, 0, 0, buf->width, buf->height);
+    xwl_window->tawc_frame_callback = wl_surface_frame(xwl_window->surface);
+    wl_callback_add_listener(xwl_window->tawc_frame_callback,
+                             &tawc_frame_listener, xwl_window);
+    wl_surface_commit(xwl_window->surface);
+}
+
+static struct xwl_tawc_buffer *
+tawc_queue_pop(struct xwl_window *xwl_window)
+{
+    struct xwl_tawc_buffer *buf = xwl_window->tawc_queue_head;
+
+    if (!buf)
+        return NULL;
+    xwl_window->tawc_queue_head = buf->queue_next;
+    if (!xwl_window->tawc_queue_head)
+        xwl_window->tawc_queue_tail = NULL;
+    xwl_window->tawc_queue_len--;
+    buf->queue_next = NULL;
+    return buf;
+}
+
+static void
+tawc_frame_done(void *data, struct wl_callback *callback, uint32_t time)
+{
+    struct xwl_window *xwl_window = data;
+    struct xwl_tawc_buffer *buf;
+
+    (void) time;
+    wl_callback_destroy(callback);
+    xwl_window->tawc_frame_callback = NULL;
+
+    buf = tawc_queue_pop(xwl_window);
+    if (buf)
+        xwl_tawc_commit(xwl_window, buf);
+}
+
+void
+xwl_tawc_window_teardown(struct xwl_window *xwl_window)
+{
+    struct xwl_tawc_buffer *buf;
+
+    if (xwl_window->tawc_frame_callback) {
+        wl_callback_destroy(xwl_window->tawc_frame_callback);
+        xwl_window->tawc_frame_callback = NULL;
+    }
+    while ((buf = tawc_queue_pop(xwl_window))) {
+        /* Never committed, so no compositor release will come; free the
+         * client's slot ourselves so it isn't stranded until its 500ms
+         * fallback. */
+        tawc_dri_send_buffer_release(buf->window_id, buf->client_mask,
+                                     buf->serial);
+        xwl_tawc_buffer_destroy(buf);
+    }
+}
+
+/* TAWC-DRI step 3: client-shipped native_handle → AHB → wl_buffer →
+ * direct attach + commit on the X11 window's wl_surface, paced by
+ * wl_surface.frame (see the frame-pacing block above).
+ *
+ * This bypasses the X server's normal pixmap flow: rather than wrap the
+ * AHB as an X pixmap and let xwl_window_present_pixmap handle the
+ * compositor handoff, we attach to the wl_surface ourselves. That means
+ * server-side X11 drawing on the same window won't see this AHB
+ * (no XGetImage, no XCopyArea integration). Pure GL clients don't do
+ * those, which is the Phase 2 use case. Mixed-rendering would race the
+ * X server's normal SHM root pixmap attach over this one.
+ *
+ * Lifecycle: a fresh xwl_tawc_buffer + AHB + wl_buffer is allocated per
+ * present. Each wl_buffer carries `buffer_release_listener`, which
+ * destroys the trio when the compositor releases its reference. That
+ * keeps the live working set bounded by the compositor's queue depth
+ * (typically ≤3) regardless of how many frames the client renders.
+ */
+int
+xwl_tawc_present_native_handle(WindowPtr window,
+                               int *fds, int num_fds,
+                               const int32_t *ints, int num_ints,
+                               int width, int height, int stride,
+                               int format, uint64_t usage,
+                               uint32_t client_mask,
+                               uint32_t serial)
+{
+    struct xwl_screen *xwl_screen;
+    struct xwl_window *xwl_window;
+    struct tawc_native_handle *handle = NULL;
+    struct tawc_ahb_desc desc;
+    struct AHardwareBuffer *ahb = NULL;
+    struct xwl_tawc_buffer *buf = NULL;
+    struct wl_buffer *wlbuf;
+    size_t handle_sz;
+    int rc = BadAlloc;
+
+    if (!window || num_fds < 0 || num_ints < 0 || width <= 0 || height <= 0)
+        goto err_close_fds;
+
+    xwl_screen = xwl_screen_get(window->drawable.pScreen);
+    if (!xwl_screen->tawc_wlegl) {
+        ErrorF("xwl_tawc: TAWC-DRI Present without android_wlegl bound\n");
+        rc = BadMatch;
+        goto err_close_fds;
+    }
+
+    xwl_window = xwl_window_from_window(window);
+    if (!xwl_window || !xwl_window->surface) {
+        ErrorF("xwl_tawc: TAWC-DRI Present on unmapped/unsurfaced window 0x%lx\n",
+               (unsigned long)window->drawable.id);
+        rc = BadMatch;
+        goto err_close_fds;
+    }
+
+    /* Build a wire-format native_handle: header + numFds fds + numInts ints. */
+    handle_sz = sizeof(*handle) +
+                (size_t)(num_fds + num_ints) * sizeof(int);
+    handle = malloc(handle_sz);
+    if (!handle)
+        goto err_close_fds;
+    handle->version = (int)sizeof(*handle);
+    handle->numFds  = num_fds;
+    handle->numInts = num_ints;
+    for (int i = 0; i < num_fds;  i++) handle->data[i]           = fds[i];
+    for (int i = 0; i < num_ints; i++) handle->data[num_fds + i] = ints[i];
+
+    memset(&desc, 0, sizeof(desc));
+    desc.width  = (uint32_t)width;
+    desc.height = (uint32_t)height;
+    desc.layers = 1;
+    desc.format = (uint32_t)format;
+    desc.usage  = usage;
+    desc.stride = (uint32_t)stride;
+
+    /* AHB METHOD_REGISTER takes ownership of the entire handle struct
+     * AND its fds — same ABI as compositor/native/wlegl_import.c. Do
+     * NOT free(handle) on success: the underlying GraphicBuffer
+     * close+deletes the handle when the AHB is released, and a manual
+     * free trips Scudo's chunk-state check. */
+    if (xwl_tawc_ahb_create_from_handle(&desc, handle, &ahb) != 0) {
+        rc = BadValue;
+        goto err_free_handle;
+    }
+    handle = NULL;
+    /* Caller's fds are now owned by the AHB; don't close them. */
+    for (int i = 0; i < num_fds; i++) fds[i] = -1;
+
+    buf = calloc(1, sizeof(*buf));
+    if (!buf) {
+        xwl_tawc_ahb_release(ahb);
+        return BadAlloc;
+    }
+    buf->ahb    = ahb;
+    buf->width  = width;
+    buf->height = height;
+    buf->stride = stride;
+    buf->format = format;
+    buf->usage  = usage;
+    buf->window_id   = (uint32_t)window->drawable.id;
+    buf->client_mask = client_mask;
+    buf->serial      = serial;
+
+    wlbuf = xwl_tawc_buffer_get_wl_buffer(xwl_screen, buf);
+    if (!wlbuf) {
+        xwl_tawc_buffer_destroy(buf);
+        return BadAlloc;
+    }
+
+    /* Tie buffer lifetime to the compositor's release event so we don't
+     * leak an FD per present. See `buffer_release_listener` block. */
+    wl_buffer_add_listener(wlbuf, &buffer_release_listener, buf);
+
+    /* Direct wl_surface attach — bypasses the X-side pixmap flow (see
+     * the comment above this function) — paced by wl_surface.frame (see
+     * the frame-pacing block). */
+    if (xwl_window->tawc_frame_callback) {
+        if (xwl_window->tawc_queue_len >= TAWC_MAX_QUEUED) {
+            /* Callbacks aren't flowing (hidden window) or the client
+             * outruns even the queue; drop the oldest, silently. */
+            xwl_tawc_buffer_destroy(tawc_queue_pop(xwl_window));
+        }
+        if (xwl_window->tawc_queue_tail)
+            xwl_window->tawc_queue_tail->queue_next = buf;
+        else
+            xwl_window->tawc_queue_head = buf;
+        xwl_window->tawc_queue_tail = buf;
+        xwl_window->tawc_queue_len++;
+    }
+    else
+        xwl_tawc_commit(xwl_window, buf);
+
+    /* The compositor side logs `wlegl: create_buffer ...` and
+     * `wlegl: imported ANativeWindowBuffer as texture ...` per AHB
+     * import; logging here would be redundant noise (a 60 fps client
+     * would fire it once per frame). Keep silent on the success path. */
+    return Success;
+
+ err_free_handle:
+    free(handle);
+ err_close_fds:
+    for (int i = 0; i < num_fds; i++)
+        if (fds[i] >= 0) close(fds[i]);
+    return rc;
+}
diff --git a/hw/xwayland/xwayland-tawc.h b/hw/xwayland/xwayland-tawc.h
new file mode 100644
index 000000000..1c2e584b9
--- /dev/null
+++ b/hw/xwayland/xwayland-tawc.h
@@ -0,0 +1,99 @@
+/*
+ * SPDX-License-Identifier: MIT
+ *
+ * TAWC AHB buffer transport for Xwayland.
+ *
+ * Wraps an AHardwareBuffer as a wl_buffer via the android_wlegl
+ * protocol so the compositor can import it through its existing
+ * gralloc1 path (compositor/native/wlegl_import.c). The protocol is
+ * the same one libhybris's wayland-platform plugin uses
+ * (libhybris/hybris/platforms/wayland/wayland_window_common.cpp:59),
+ * which is what the compositor already accepts from ordinary Wayland
+ * clients.
+ *
+ * See notes/xwayland.md.
+ */
+
+#ifndef XWAYLAND_TAWC_H
+#define XWAYLAND_TAWC_H
+
+#include "scrnintstr.h"
+#include "pixmap.h"
+
+struct AHardwareBuffer;
+struct wl_buffer;
+struct xwl_screen;
+struct xwl_window;
+
+/* AHB-backed buffer + lazily-created wl_buffer. xwl_tawc_present_native_handle
+ * allocates one per TAWC-DRI present and ties its lifetime to the
+ * compositor's wl_buffer.release event. */
+struct xwl_tawc_buffer {
+    struct AHardwareBuffer *ahb;   /* Owned. */
+    struct wl_buffer *buffer;      /* Lazy; created on first
+                                    * xwl_tawc_buffer_get_wl_buffer. */
+    int width;
+    int height;
+    int stride;     /* In pixels, as reported by AHardwareBuffer_describe. */
+    int format;     /* Android pixel format (AHARDWAREBUFFER_FORMAT_*). */
+    uint64_t usage; /* AHB usage flags. */
+    /* For TAWCDRIBufferRelease routing: the presenting window's XID and
+     * the presenting client's clientAsMask (serials are per-client, so
+     * the release must not be broadcast to other selectors on the same
+     * window), plus the client-chosen serial echoed back. */
+    uint32_t window_id;
+    uint32_t client_mask;
+    uint32_t serial;
+    /* Link in xwl_window's frame-pacing FIFO while the present waits
+     * for a wl_surface.frame callback slot. NULL once committed. */
+    struct xwl_tawc_buffer *queue_next;
+};
+
+/* Tear down: destroys the wl_buffer (if any), releases the AHB, frees
+ * the xwl_tawc_buffer struct. */
+void xwl_tawc_buffer_destroy(struct xwl_tawc_buffer *buf);
+
+/* Lazily ship the AHB to the compositor via android_wlegl and return
+ * the resulting wl_buffer. Returns NULL if the compositor doesn't
+ * advertise android_wlegl, or buffer creation fails. */
+struct wl_buffer *
+xwl_tawc_buffer_get_wl_buffer(struct xwl_screen *xwl_screen,
+                              struct xwl_tawc_buffer *buf);
+
+/* Stub: routes an AHB-backed PixmapPtr through the same shipping path.
+ * Returns NULL because TAWC-DRI bypasses the pixmap router and attaches
+ * its wl_buffer directly to the X11 window's wl_surface. */
+struct wl_buffer *xwl_tawc_pixmap_get_wl_buffer(PixmapPtr pixmap);
+
+/* TAWC-DRI entry point. Reconstructs an AHardwareBuffer from a
+ * client-shipped native_handle (numFds fds + numInts ints), wraps it
+ * as an xwl_tawc_buffer, and attaches + commits the resulting
+ * wl_buffer directly on the X11 window's wl_surface — immediately if
+ * the surface is idle, else queued behind the pending wl_surface.frame
+ * callback (one commit per display frame). Takes ownership of the
+ * fds in `fds[0..num_fds]` on success (closes them on failure too —
+ * AHardwareBuffer_createFromHandle's REGISTER mode owns them on success
+ * via the AHB; on failure we close ourselves).
+ *
+ * `serial` is the client-chosen PresentBuffer cookie (v0.3); it is
+ * echoed back in a TAWCDRIBufferRelease event to the presenting client
+ * (identified by `client_mask` = clientAsMask) when the compositor
+ * releases the wl_buffer, if that client selected for release events.
+ *
+ * Returns 0 on success, X protocol error code (BadValue / BadMatch /
+ * BadAlloc) on failure. */
+int xwl_tawc_present_native_handle(WindowPtr window,
+                                   int *fds, int num_fds,
+                                   const int32_t *ints, int num_ints,
+                                   int width, int height, int stride,
+                                   int format, uint64_t usage,
+                                   uint32_t client_mask,
+                                   uint32_t serial);
+
+/* Free the window's frame-pacing state: cancel the pending
+ * wl_surface.frame callback and drop queued presents (their clients
+ * get BufferRelease so the buffer slots aren't stranded). Called from
+ * xwl_window_dispose before the wl_surface goes away. */
+void xwl_tawc_window_teardown(struct xwl_window *xwl_window);
+
+#endif /* XWAYLAND_TAWC_H */
diff --git a/hw/xwayland/xwayland-window.c b/hw/xwayland/xwayland-window.c
index 4a9a58a2f..b53421d94 100644
--- a/hw/xwayland/xwayland-window.c
+++ b/hw/xwayland/xwayland-window.c
@@ -48,6 +48,9 @@
 #include "xwayland-window-buffers.h"
 #include "xwayland-shm.h"
 #include "xwayland-dmabuf.h"
+#ifdef TAWC
+#include "xwayland-tawc.h"
+#endif
 
 #include "linux-dmabuf-unstable-v1-client-protocol.h"
 #include "tearing-control-v1-client-protocol.h"
@@ -1730,6 +1733,9 @@ xwl_window_dispose(struct xwl_window *xwl_window)
     if (xwl_window->surface_sync)
         wp_linux_drm_syncobj_surface_v1_destroy(xwl_window->surface_sync);
 
+#ifdef TAWC
+    xwl_tawc_window_teardown(xwl_window);
+#endif
     release_wl_surface_for_window(xwl_window);
     xorg_list_del(&xwl_window->link_damage);
     xorg_list_del(&xwl_window->link_window);
diff --git a/hw/xwayland/xwayland-window.h b/hw/xwayland/xwayland-window.h
index ac3ce5034..b16f4bd38 100644
--- a/hw/xwayland/xwayland-window.h
+++ b/hw/xwayland/xwayland-window.h
@@ -43,6 +43,8 @@
 #include "xwayland-types.h"
 #include "xwayland-dmabuf.h"
 
+struct xwl_tawc_buffer;
+
 struct xwl_wl_surface {
     OsTimerPtr wl_surface_destroy_timer;
     struct wl_surface *wl_surface;
@@ -106,6 +108,16 @@ struct xwl_window {
     struct wp_fractional_scale_v1 *fractional_scale;
     int fractional_scale_numerator;
     struct wp_linux_drm_syncobj_surface_v1 *surface_sync;
+
+    /* TAWC-DRI frame pacing (hw/xwayland/xwayland-tawc.c): commits are
+     * throttled to the compositor's wl_surface.frame cadence. While a
+     * callback is outstanding, presents queue FIFO here; one is
+     * committed per callback. All NULL/0 unless the window receives
+     * TAWC-DRI presents; torn down in xwl_window_dispose. */
+    struct wl_callback *tawc_frame_callback;
+    struct xwl_tawc_buffer *tawc_queue_head;
+    struct xwl_tawc_buffer *tawc_queue_tail;
+    int tawc_queue_len;
 };
 
 struct xwl_window *xwl_window_get(WindowPtr window);
diff --git a/include/extinit.h b/include/extinit.h
index 59d18962f..10863ee4d 100644
--- a/include/extinit.h
+++ b/include/extinit.h
@@ -158,6 +158,10 @@ extern void XvMCExtensionInit(void);
 extern void dri3_extension_init(void);
 #endif
 
+#ifdef TAWC
+extern void tawc_dri_extension_init(void);
+#endif
+
 #if defined(PRESENT)
 #include "presentext.h"
 #endif
diff --git a/include/meson.build b/include/meson.build
index 72e1e5d5b..b4237fcb4 100644
--- a/include/meson.build
+++ b/include/meson.build
@@ -214,6 +214,7 @@ conf_data.set('RES', build_res ? '1' : false)
 conf_data.set('RENDER', '1')
 conf_data.set('SCREENSAVER', build_screensaver ? '1' : false)
 conf_data.set('SHAPE', '1')
+conf_data.set('TAWC', build_tawc ? '1' : false)
 conf_data.set('XACE', build_xace ? '1' : false)
 conf_data.set('XCMISC', '1')
 conf_data.set('XCSECURITY', build_xsecurity ? '1' : false)
diff --git a/meson.build b/meson.build
index 479b3025a..b6830f66a 100644
--- a/meson.build
+++ b/meson.build
@@ -360,6 +360,8 @@ endif
 
 build_dpms = get_option('dpms')
 
+build_tawc = get_option('tawc')
+
 build_xf86bigfont = get_option('xf86bigfont')
 build_screensaver = get_option('screensaver')
 build_res = get_option('xres')
diff --git a/meson_options.txt b/meson_options.txt
index 1acc65ead..095a89caf 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -86,3 +86,6 @@ option('devel-docs', type: 'combo', choices: ['true', 'false', 'auto'], value: '
         description: 'Build development documentation')
 option('docs-pdf', type: 'combo', choices: ['true', 'false', 'auto'], value: 'auto',
         description: 'Whether to build PDF version of documentation. Setting is ignored if documentation is not built.')
+
+option('tawc', type: 'boolean', value: false,
+       description: 'Enable TAWC AHB-everywhere transport (TAWC-DRI extension + xwayland-tawc.c)')
diff --git a/mi/miinitext.c b/mi/miinitext.c
index 1ddcd7fda..547130051 100644
--- a/mi/miinitext.c
+++ b/mi/miinitext.c
@@ -149,6 +149,9 @@ static const ExtensionModule staticExtensions[] = {
 #ifdef DRI3
     {dri3_extension_init, "DRI3", NULL},
 #endif
+#ifdef TAWC
+    {tawc_dri_extension_init, "TAWC-DRI", NULL},
+#endif
 #ifdef RES
     {ResExtensionInit, "X-Resource", &noResExtension},
 #endif
diff --git a/present/present_scmd.c b/present/present_scmd.c
index ba7d7cb65..a8b10eddf 100644
--- a/present/present_scmd.c
+++ b/present/present_scmd.c
@@ -57,6 +57,29 @@ present_flip_pending_pixmap(ScreenPtr screen)
     return screen_priv->flip_pending->pixmap;
 }
 
+/* tawc instrumentation: trace which gating predicate inside
+ * present_check_flip() forces a copy-mode present. Compiled in
+ * unconditionally (Xwayland's ErrorF() goes to stderr → captured by
+ * the compositor's logcat tag). The cost is one printf per Present —
+ * negligible vs the actual present pipeline, and the data is the
+ * load-bearing diagnostic for "is the EGL-X11 path picking flip?".
+ *
+ * NOTE: the libhybris EGL-X11 plugin (Phase 2 step 4) sends
+ * TAWCDRIPresentBuffer over its own X11 wire and the X server attaches
+ * the wl_buffer to the window's wl_surface directly, bypassing the
+ * Present extension entirely. So for our pipe these printfs never
+ * fire. They DO fire for any other client that goes via standard
+ * Present.PresentPixmap (DRI3-style), e.g. a hypothetical mesa
+ * software-rasteriser path, or a Wine GL game that uses Present
+ * instead of our TAWC-DRI extension.
+ */
+#define TAWC_FLIP_FAIL(why) do { \
+        ErrorF("xwl_present_flip: copy mode (window=0x%08" PRIx32 \
+               " pixmap=0x%08" PRIx32 ") - %s\n", \
+               (uint32_t)window->drawable.id, \
+               (uint32_t)(pixmap ? pixmap->drawable.id : 0), why); \
+    } while (0)
+
 static Bool
 present_check_flip(RRCrtcPtr            crtc,
                    WindowPtr            window,
@@ -79,18 +102,26 @@ present_check_flip(RRCrtcPtr            crtc,
     if (reason)
         *reason = PRESENT_FLIP_REASON_UNKNOWN;
 
-    if (!screen_priv)
+    if (!screen_priv) {
+        TAWC_FLIP_FAIL("no screen_priv");
         return FALSE;
+    }
 
-    if (!screen_priv->info)
+    if (!screen_priv->info) {
+        TAWC_FLIP_FAIL("no screen_priv->info");
         return FALSE;
+    }
 
-    if (!crtc)
+    if (!crtc) {
+        TAWC_FLIP_FAIL("no crtc");
         return FALSE;
+    }
 
     /* Check to see if the driver supports flips at all */
-    if (!screen_priv->info->flip)
+    if (!screen_priv->info->flip) {
+        TAWC_FLIP_FAIL("driver has no flip");
         return FALSE;
+    }
 
     /* Ask the driver for permission. Do this now to see if there's TearFree. */
     if (screen_priv->info->version >= 1 && screen_priv->info->check_flip2) {
@@ -103,12 +134,16 @@ present_check_flip(RRCrtcPtr            crtc,
             if (!reason || tmp_reason != PRESENT_FLIP_REASON_BUFFER_FORMAT) {
                 if (reason)
                     *reason = tmp_reason;
+                ErrorF("xwl_present_flip: copy mode (window=0x%08" PRIx32
+                       ") - driver check_flip2 rejected (reason=%d)\n",
+                       (uint32_t)window->drawable.id, tmp_reason);
                 return FALSE;
             }
         }
     } else if (screen_priv->info->check_flip) {
         if (!(*screen_priv->info->check_flip) (crtc, window, pixmap, sync_flip)) {
             DebugPresent(("\td %08" PRIx32 " -> %08" PRIx32 "\n", window->drawable.id, pixmap ? pixmap->drawable.id : 0));
+            TAWC_FLIP_FAIL("driver check_flip rejected");
             return FALSE;
         }
     }
@@ -117,21 +152,26 @@ present_check_flip(RRCrtcPtr            crtc,
     window_pixmap = screen->GetWindowPixmap(window);
     if (window_pixmap != screen->GetScreenPixmap(screen) &&
         window_pixmap != screen_priv->flip_pixmap &&
-        window_pixmap != present_flip_pending_pixmap(screen))
+        window_pixmap != present_flip_pending_pixmap(screen)) {
+        TAWC_FLIP_FAIL("window redirected (Composite)");
         return FALSE;
+    }
 
     /* Check for full-screen window */
     if (!RegionEqual(&window->clipList, &root->winSize)) {
+        TAWC_FLIP_FAIL("window not fullscreen");
         return FALSE;
     }
 
     /* Source pixmap must align with window exactly */
     if (x_off || y_off) {
+        TAWC_FLIP_FAIL("source pixmap offset");
         return FALSE;
     }
 
     /* Make sure the area marked as valid fills the screen */
     if (valid && !RegionEqual(valid, &root->winSize)) {
+        TAWC_FLIP_FAIL("valid region != screen");
         return FALSE;
     }
 
@@ -142,15 +182,21 @@ present_check_flip(RRCrtcPtr            crtc,
 #endif
         window->drawable.width != pixmap->drawable.width ||
         window->drawable.height != pixmap->drawable.height) {
+        TAWC_FLIP_FAIL("window/pixmap geometry mismatch");
         return FALSE;
     }
 
     if (tmp_reason == PRESENT_FLIP_REASON_BUFFER_FORMAT) {
         if (reason)
             *reason = tmp_reason;
+        TAWC_FLIP_FAIL("buffer format reason");
         return FALSE;
     }
 
+    ErrorF("xwl_present_flip: FLIP OK (window=0x%08" PRIx32
+           " pixmap=0x%08" PRIx32 ")\n",
+           (uint32_t)window->drawable.id,
+           (uint32_t)(pixmap ? pixmap->drawable.id : 0));
     return TRUE;
 }
 
